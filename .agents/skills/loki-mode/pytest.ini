[pytest]
# Exclude Docker integration tests by default -- they require a live server
# Run with: pytest tests/docker/ to execute Docker tests explicitly
testpaths = tests
norecursedirs = tests/docker tests/fixtures
asyncio_mode = auto
